[pytest]
pythonpath = backend .
testpaths = ci_tests
markers =
    integration: live services
    asyncio: async test driven by pytest-asyncio
    mcp: MCP protocol conformance (JSON-RPC lifecycle over streamable-HTTP)
