services:
  la-backend:
    working_dir: /workspace
    environment:
      PYTHONPATH: /workspace/backend:/workspace
    volumes:
      - ./:/workspace
    command: ["sleep", "infinity"]
    healthcheck:
      disable: true
