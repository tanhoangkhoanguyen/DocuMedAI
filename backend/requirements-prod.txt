# Torch & Transformers
--extra-index-url https://download.pytorch.org/whl/cpu

torch
transformers
tokenizers
sentence-transformers
huggingface_hub
safetensors

# Math & Data
numpy
scipy           # transitive: sentence-transformers
scikit-learn    # transitive: sentence-transformers

# Vector DB clients
qdrant-client==1.18.0
pymongo
redis

# Web / API
fastapi
python-multipart            # multipart/form-data parsing for document upload (UploadFile + Form)
uvicorn
httpx
aiohttp

# Tests (services + API)
pytest==8.3.5
pytest-asyncio==0.25.3

# LangChain
langchain
langchain-community
langchain-openai
langchain-google-genai
langchain-huggingface
langchain-text-splitters
langgraph
langgraph-checkpoint
langgraph-prebuilt
langgraph-sdk
langsmith

# LLM Providers
openai
google-genai
tiktoken

# Multi-agent orchestration
crewai
# CrewAI's native providers only accept known OpenAI/Gemini/etc. model names.
# We route a Gemini model through the OpenAI-compatible LLM proxy as
# "openai/gemini-2.5-flash", which fails native validation, so CrewAI falls back
# to LiteLLM (its OpenAI-compatible custom-endpoint path). That fallback is an
# optional dependency — install it explicitly or startup raises ImportError.
litellm
mcp

# Database / ORM
SQLAlchemy

# Utilities
python-dotenv
pydantic
pydantic-settings
pypdf
python-docx                # .docx text extraction for user document upload
arq                        # asyncio Redis job queue for async document ingestion
PyYAML
bcrypt
cryptography
pytz
tenacity
requests
PyJWT

# Optional performance/runtime
uvloop
httptools
watchfiles
orjson
