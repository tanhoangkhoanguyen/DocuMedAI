name: LLMGuard CI

# Scoped to the Go service by path so it stays off the critical path for the
# Python and frontend workflows, which never touch this code.
on:
  pull_request:
    paths:
      - "backend/llmguard/**"
      - ".github/workflows/llmguard-ci.yml"
  push:
    branches:
      - main
      - develop
      - feature/Setup-CI
    paths:
      - "backend/llmguard/**"
      - ".github/workflows/llmguard-ci.yml"

permissions:
  contents: read

# A new push to the same PR cancels the previous run — this workflow gates a
# small service and there is no value in finishing a superseded run.
concurrency:
  group: llmguard-ci-${{ github.ref }}
  cancel-in-progress: true

defaults:
  run:
    working-directory: backend/llmguard

jobs:
  go:
    name: test + lint
    runs-on: ubuntu-latest
    timeout-minutes: 15

    services:
      # The rate limiter's token bucket is a Lua script, so it can only be
      # covered with a real server. testutil.RequireRedis skips when Redis is
      # absent (a laptop), which means CI is the environment that has to supply
      # it or those tests would silently never run.
      redis:
        image: redis:7-alpine
        ports:
          - 6379:6379
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 5s
          --health-timeout 3s
          --health-retries 10

      # The usage writer's insert path is columnar block encoding done inside the
      # driver, so a fake sink can prove the buffer logic but never that a row
      # survives the wire format with its types intact.
      # testutil.RequireClickHouse skips when it is absent (a laptop), which means
      # CI has to supply it or the round-trip test would silently never run
      # anywhere — the same reasoning as the Redis service above.
      clickhouse:
        image: clickhouse/clickhouse-server:24.8-alpine
        ports:
          - 9000:9000
          - 8123:8123
        options: >-
          --health-cmd "wget --spider -q http://127.0.0.1:8123/ping"
          --health-interval 5s
          --health-timeout 3s
          --health-retries 20

    env:
      # DB 15, matching testutil.DefaultRedisURL — never the app's DB 0 or the
      # proxy's own DB 1.
      TEST_REDIS_URL: redis://127.0.0.1:6379/15
      # The NATIVE port. Tests create and use their own llmguard_test database
      # (testutil.TestClickHouseDatabase), never the gateway's own.
      TEST_CLICKHOUSE_ADDR: 127.0.0.1:9000

    steps:
      - uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          # Read the version from go.mod so the toolchain can never drift from
          # what the module declares.
          go-version-file: backend/llmguard/go.mod
          cache-dependency-path: backend/llmguard/go.sum

      # `make lint` installs the pinned golangci-lint via `go install` when it is
      # missing. Caching GOBIN keeps that off the clock on every run but the
      # first, while the Makefile stays the single source of the version.
      - name: Cache golangci-lint binary
        uses: actions/cache@v4
        with:
          path: ~/go/bin/golangci-lint
          key: golangci-lint-${{ runner.os }}-v2.1.6

      - name: Download modules
        run: go mod download

      # Fails if go.mod/go.sum are not what the source actually needs, so a
      # forgotten `go mod tidy` is caught here rather than at the next build.
      - name: Verify go.mod is tidy
        run: |
          go mod tidy
          git diff --exit-code -- go.mod go.sum

      # Passes with "no test files" until the first _test.go lands. The race
      # detector switches itself on here because CI has cgo.
      - name: Test
        run: make test

      - name: Lint
        run: make lint
